'use strict';

module.exports = function(Portal) {

  Portal.getCarDetails = function(nationalId)=>{
    //send req to sdb get plateNumber


    //send two req to traffic and court to get plateNumber details
  }
};
